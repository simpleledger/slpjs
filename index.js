'use strict';

const slp = require('./lib/slp');
const utils = require('./lib/utils');
const bitbox = require('./lib/bitboxnetwork');

module.exports = {
    slp: slp,
    utils: utils,
    bitbox: bitbox
}
'use strict';

const slp = require('./lib/slp');
const slputils = require('./lib/slputils');
const bitbox = require('./lib/bitboxnetwork');

module.exports = {
    slp: slp,
    slputils: slputils,
    bitbox: bitbox
}
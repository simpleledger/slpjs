'use strict';

const slp = require('./lib/slp');
const slputils = require('./lib/slputils');
const network = require('./lib/network');

module.exports = {
    slp: slp,
    slputils: slputils,
    network: network
}